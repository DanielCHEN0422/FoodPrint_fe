# 使用 pnpm 作为包管理器
# 启用严格的对等依赖检查
strict-peer-dependencies=false
# 自动安装对等依赖
auto-install-peers=true
# 使用符号链接（更快）
symlink=true
# 避免 Windows 下 Metro 监视临时目录失败
node-linker=hoisted
public-hoist-pattern[]=*
# 确保在线模式
offline=false
